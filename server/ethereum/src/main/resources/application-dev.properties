spring.config.import=optional:file:.env[.properties]
logging.level.ethereum.tutorials.java.ethereum=DEBUG

firebase.project.id=${FIREBASE_PROJECT_ID}
firebase.app.id=${FIREBASE_APP_ID}
firebase.storage.bucket=${FIREBASE_STORAGE_BUCKET}
firebase.api.key=${FIREBASE_API_KEY}
firebase.auth.domain=${FIREBASE_AUTH_DOMAIN}
firebase.messaging.sender.id=${FIREBASE_MSGGING_SENDER_ID}
firebase.measurement.id=${FIREBASE_MEASUREMENT_ID}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
# spring.datasource.hikari.connectionTimeout=20000
# spring.datasource.hikari.maximumPoolSize=5

### railway somehow needs this ###
# spring.data.mongodb.host=${MONGOHOST}
# spring.data.mongodb.port=${MONGOPORT}
# spring.data.mongodb.username=${MONGOUSER}
# spring.data.mongodb.password=${MONGOPASSWORD}
### railway somehow needs this ###

spring.data.mongodb.authentication-database=admin
spring.data.mongodb.database=${MONGODB}
spring.data.mongodb.url=${MONGO_URL}

google.app.cred.filepath=${GOOGLE_APPLICATION_CREDENTIALS}

etherscan.api.key=${ETHERSCAN_API_KEY}
etherscan.api.url=https://api.etherscan.io/api

client.url=${CLIENT_URL}

wallet.public.key=${GANACHE_PUBLIC_KEY}
wallet.private.key=${GANACHE_PRIVATE_KEY}

crowdfunding.factory.contract.address=${DEV_CROWDFUNDING_FACTORY_CONTRACT_ADDRESS}
faucet.contract.address=${DEV_FAUCET_CONTRACT_ADDRESS}

rpc.url=${DEV_RPC}
chain.id=${CHAIN_ID}

# App Properties
fuchureBezkoder.app.jwtCookieName=bezkoder
fuchureBezkoder.app.jwtSecret=bezKoderSecretKey
fuchureBezkoder.app.jwtExpirationMs=86400000